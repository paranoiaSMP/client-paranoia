org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

mod_version=0.3.0

# Loom est la seule version que la CI ne peut pas resoudre toute seule: elle est
# lue au moment d'appliquer le plugin, avant tout script. Les versions de
# Minecraft, yarn, loader et Fabric API sont ecrites par
# scripts/resolve-fabric-versions.mjs dans versions/<mc>/versions.properties.
loom_version=1.11-SNAPSHOT
