org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

mod_version=0.3.0

# Toutes les versions amont sont resolues par scripts/resolve-fabric-versions.mjs
# avant le build: yarn, loader et Fabric API dans versions/<mc>/versions.properties,
# et loom directement ici -- Gradle lit ce fichier avant d'executer le moindre
# script, donc un fichier annexe arriverait trop tard.
# La valeur commitee n'est qu'un point de depart; le script la remplace.
loom_version=1.11.9
